import { FormData } from '../SignUp.types';
type Props = {
    formData: FormData;
};
export declare const validate: ({ formData }: Props) => import('zod').ZodSafeParseResult<{
    firstName: string;
    lastName: string;
    password: string;
    passwordCheck: string;
}>;
export {};
