{
  "name": "backend",
  "version": "1.0.0",
  "description": "server for flowers",
  "author": "10011101",
  "license": "ISC",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "dev": "node --loader ts-node/esm src/index.ts",
    "prisma:migrate:dev": "prisma migrate dev",
    "prisma:generate:dev": "prisma generate",
    "build": "tsc",
    "postinstall": "prisma generate --schema ./prisma/schema.prisma",
    "start": "node dist/index.js",
    "prisma:migrate:deploy": "prisma migrate deploy",
    "prisma:generate:deploy": "prisma generate --schema ./prisma/schema.prisma"
  },
  "dependencies": {
    "@imforked/legos": "1.0.1",
    "@prisma/client": "^6.18.0",
    "express": "^5.1.0",
    "zod": "^4.1.8"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/node": "^24.3.1",
    "dotenv": "^17.2.3",
    "prisma": "^6.18.0",
    "ts-node": "^10.9.2",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^5.9.2"
  }
}
